//warning:
//use actual versions below, each version seems to break other tests
//yarn add --dev jest@~26.6.3 jest-expo@~41.0.0 jest-fetch-mock@~3.0.3 @testing-library/react-native@~7.2.0

test("it works", () => {
  expect(true).toBeTruthy();
});
